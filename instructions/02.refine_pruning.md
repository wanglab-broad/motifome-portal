The current webpages are really nice. But I want to do a few more changes to make it more user-friendly. 

1. don't show "About" section, remove all content related to them.
2. For home page, change title to "MIRTO Human Motifome". remove the text "MIRTO reads a whole mRNA while conditioned on the protein it encodes. Two of its internals are readable: where attention concentrates, and how surprised the model is by each nucleotide (NTScore). Together they localise 889,215 motif instances in the 5′UTR, 3′UTR and protein sequence of 18,093 human transcripts. Instances were embedded and k-means’d into 900 clusters, every protein cluster was tested against every UTR cluster for co-occurrence across genes (NPMI with an APC background correction), and the survivors of a phylogenetic-independence gate were organised into six modules. What follows is that evidence, in full — including every place it runs out."
3. for homw page, remove all statistics like "Motif instances
889,215
5′UTR, 3′UTR and protein
Transcripts
18,093
17,847 gene symbols
Motif clusters
900
519 carry a gated association
Gated associations
2,620
757 cross a module boundary"
4. remove "Methods & caveats"
5. change sentence "Modules are named after the GO terms enriched in their carrier genes — the name describes the genes, not the motifs." to "Modules are named after the GO terms enriched in their carrier genes"
6. remove section "the gate" and remove section "Most candidate associations did not survive."
7. remove "What this atlas will not claim"
8. remove "Start from a gene, a cluster, or the graph"
9. for "Browse" page, remove "All 900 motif clusters
300 protein, 500 3′UTR and 100 5′UTR clusters, built by k-means over MIRTO embeddings. Every facet count below is recomputed against the other active filters, so an empty combination announces itself before you pick it.". just say "Motif clusters"
10. remove statistics "900
of 900 clusters · 456 with a logo · 519 with a strict partner"
11. for network pages, remove "519 nodes · 2,620 gated edges · 757 cross-module (28.9%)"
12. remove the banner "Module membership is statistical co-occurrence of motif clusters across genes — not demonstrated physical interaction. 2,620 of 166,615 co-occurring pairs (1.6%) pass the phylogenetic-independence gate. They are candidate, unvalidated pairs, not confirmed interactions. Why this gate?
"
13. remove text "One connected graph of 519 motif clusters. Protein clusters on one side, UTR clusters on the other; an edge is a co-occurrence that survived the phylogenetic-independence gate. 757 of them cross a module boundary, so the six modules are not six separate pictures — they are one giant component of 513 nodes plus three isolated dyads."
14. remove text: "Directional. Row = the protein cluster’s module, column = the UTR cluster’s module. M1→M2 is 178 edges while M2→M1 is 66 — a symmetric meta-graph would erase that. Excludes 3 edges whose endpoints sit outside every module.
6 of 519 clusters sit outside every module. They form 3 isolated dyads and appear only in the full graph — a module drill-down cannot show them."
15. remove "npmi_raw is refused. npmi_raw regenerates the artifact leaderboard this analysis removed — 25 of its top 30 associations are ZNF-clade artifacts by the authors’ own verdict column. It is not offered as an encoding or a sort key anywhere in the atlas."; just don't show anything with npmi_raw. 
16. make searching bar bigger.
